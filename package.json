{
  "name": "lru-pool",
  "description": "A keyed pool that recycles the least-recently-used objects.",
  "version": "0.0.1",
  "keywords": [
    "mru",
    "lru",
    "cache",
    "pool"
  ],
  "scripts": {
    "pretest": "standard",
    "test": "tap test --branches=100 --functions=100 --lines=100 --statements=100"
  },
  "main": "lib/lru-pool.js",
  "repository": "git://github.com/mikemorris/node-lru-pool.git",
  "devDependencies": {
    "standard": "^6.0.8",
    "tap": "^5.7.0"
  },
  "license": "ISC",
  "dependencies": {
    "yallist": "^2.0.0"
  },
  "engines": {
    "node": ">= 0.12"
  }
}
